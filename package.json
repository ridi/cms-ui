{
  "private": true,
  "workspaces": [
    "example",
    "lib"
  ],
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build"
  },
  "devDependencies": {
    "react-app-rewire-babel-loader": "^0.1.1"
  }
}
